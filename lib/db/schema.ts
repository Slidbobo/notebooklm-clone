/**
 * Drizzle schema.
 *
 * Filled in Phase 1 (identity and data). The file exists from Phase 0 so that
 * drizzle.config.ts points at a valid module and the toolchain is provably
 * wired before any table is designed.
 */
export {};
